{
  "name": "project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:start --prefix server\" \"npm:dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
